# Default configuration for Pong environment

# Field dimensions (normalized to [0, 1])
width: 1.0
height: 1.0

# Paddle settings
paddle_height: 0.2
paddle_width: 0.02
paddle_speed: 0.05

# Ball settings
ball_radius: 0.02
ball_speed: 0.02

# Game rules
winning_score: 11
max_steps: 1000

# Rendering
render_fps: 60
